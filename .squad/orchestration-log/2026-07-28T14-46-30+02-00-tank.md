# Tank orchestration log — PR Azure Environment commissioning

- **Timestamp:** 2026-07-28T14:46:30+02:00
- **Requested by:** Benoit Moussaud
- **Implementer:** Tank (Azure Platform Engineer)
- **Session:** Commissioning the PR Azure Environment deploy pipeline

## Outcome

Tank commissioned the previously-never-green `PR Azure Environment` workflow. Run `30360924609` is green: Preflight gate, Provision and deploy, Update PR comment, and Python CI `validate` all passed. PR #45 is open, mergeable, and clean.

## Coordinator steering

- Redirected Tank away from per-module nested deployment-name patching toward the root-cause `AZURE_ENV_NAME` budget in `infra/scripts/pr_environment_names.py`.
- Required a fast static regression test enumerating Bicep module names against ARM's 64-character deployment-name cap.
- Presented the scope-drift choice to Benoit: merge the CSS-only PR immediately or continue commissioning the pipeline on this PR. Benoit chose to push through to green.

## Layered failures resolved

1. Preflight branch convention diagnostics (`invalid_names`, exit 3) surfaced.
2. GitHub immutable OIDC subject credential added for `azure-pr-app`.
3. PR Container Apps workload profile switched to Consumption semantics for scale-to-zero.
4. Per-module ARM deployment-name fixes identified as whack-a-mole.
5. Root-cause name budget set: `AZURE_ENV_NAME` capped at 40 chars (`64 - 24`).
6. Bicep `existing` resource references given explicit `dependsOn` edges.

## Outstanding for Benoit

- Orphaned RG `rg-pr-45-change-application-background-color-to-green-1a64a29b` is untagged and will not be reaped by the janitor.
- Active RG `rg-pr-45-change-application-backgr-1a64a29b` is tagged `pr-app` and expires `2026-08-04T12:58:32Z`.
- Managed ACA infra RG `ME_pr45...` remains tied to the active environment.
- Azure Cost Management returned `429`; actual cost figures were unavailable.
