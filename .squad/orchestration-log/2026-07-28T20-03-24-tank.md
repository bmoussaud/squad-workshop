# Orchestration Log Entry

> One file per agent spawn. Saved to `.squad/orchestration-log/{timestamp}-{agent-name}.md`

---

### 2026-07-28T20:03:24+02:00 — PR environment concurrency cap orphan accounting

| Field | Value |
|-------|-------|
| **Agent routed** | Tank (Azure Platform Engineer) |
| **Why chosen** | CI/PR-ephemeral-environment infrastructure failure on PR #64; app-tier provisioning was blocked by `app_concurrency_cap` at 4/3 active environments. |
| **Mode** | `background` |
| **Why this mode** | Infrastructure diagnosis and fix could proceed independently, with validation reported back to the coordinator. |
| **Files authorized to read** | `.github/workflows/pr-environment.yml`, `.github/workflows/pr-environment-janitor.yml`, `infra/scripts/`, `tests/`, `docs/design/pr-ephemeral-environments.md`, `.squad/agents/tank/history.md` |
| **File(s) agent must produce** | `.github/workflows/pr-environment.yml`, `.github/workflows/pr-environment-janitor.yml`, `infra/scripts/pr_env_active_count.py`, `tests/test_pr_env_active_count.py`, `docs/design/pr-ephemeral-environments.md`, `.squad/agents/tank/history.md` |
| **Outcome** | Completed. Root cause was orphaned app-tier resource groups from closed/closing PRs counted against the cap; fix committed as `0795dc7` on `bmoussaud-literate-umbrella`. |

---
