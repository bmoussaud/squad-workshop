# Project Context

- **Owner:** bmoussaud
- **Project:** Python application on Azure for generating fantasy trading-card-style imagery
- **Stack:** Python, Azure, generative image models
- **Created:** 2026-07-22T11:30:53+00:00

## Core Context

Agent Rai initialized and ready for work.

## Recent Updates

📌 Team initialized on 2026-07-22

## Learnings

Initial setup complete.

## Review History

- 2026-07-27: Rejected issue #16 Phase 1 preflight. Fork trust accepts ambiguous falsey signals and can proceed; Foundry exceptions also lack a mandatory review/cap gate. Independent remediation is required. Also flagged raw identifier/repository interpolation in outputs as advisory log-safety exposure.
- 2026-07-27: Approved Morpheus's independent issue #16 remediation. Strict trust signals, Foundry authorization plus cap enforcement, and log-safe projections passed focused adversarial probes and the full suite.

📌 Team update (2026-07-27T14:24:13+02:00): Cross-agent architecture outcome for #16: Tank’s deterministic names are correct but `web.bicep` currently reconstructs and overflows PR Container App names. Phase 3/#15 must consume precomputed names as parameters; no Azure PR environment should rely on the current Bicep construction.

- 2026-07-27: Reviewed authorized CI guardrail relaxation (#25, commit 51336ce). 🟢 Green. Branch-name regex demoted to advisory `::notice::`; "closes exactly one issue" kept as a hard, fail-closed gate; branch↔issue cross-check made conditional on convention. Verified independently: BRANCH/BODY still injection-safe via env + "$VAR" (no `${{ }}` in run:); `::notice::` echo of attacker-controlled `$branch` cannot inject workflow commands (refs carry no newline, commands only honoured at line-start). Bash-reproduced edge cases all fail-closed. Learning: the conditional cross-check is a typo-catcher, not a security control (bypassable by branch name) — but the commit/comments say so honestly, so it passes. Double-close-same-issue false positive is over-strict but pre-existing and fail-closed; flagged as optional dedupe recommendation.

📌 Team update (2026-07-27T16:58:24.269+02:00): Tank's CI relaxation for issue #25 (commit 51336ce) ratified GREEN by Rai. Decision merged into decisions.md. App-generated worktree branch names now pass CI without requiring manual rename. — decided by Tank, reviewed by Rai

- 2026-07-27: Reviewed Phase 3 PR deploy workflow + `pr_preflight.py` CLI (#15, commit e0d2d64) — treated as unreviewed code of unknown provenance (author lost pre-commit; coordinator self-validated). 🟢 Green. Traced fork isolation end-to-end: `head.repo.fork` -> strict `_parse_tristate_bool` -> `evaluate` step-0 bool gate -> `decision` string -> deploy `if: == 'proceed'` JOB-level exclusion. Empirically probed 9 input cases: only exact `false` proceeds; forks/empty/ambiguous -> blocked/exit3; draft -> skip (NOT proceed); empty count -> blocked. Confirmed id-token:write only on deploy, pull-requests:write only on comment, all PR values via env (no `${{ github.event.* }}` in run:), 4 actions SHA-pinned, `_sanitize_log` blocks `$GITHUB_OUTPUT` newline injection, comment never claims health without deploy-success AND smoke-pass. 186 tests pass (re-run independently). Two non-blocking advisories: (1) the smoke-message comment branch is dead code (smoke failure fails deploy job -> "Deployment did not complete" renders instead — fail-closed); (2) preflight crash = silent green no-op. Learning: this workflow's fork gate is layered (Python strict-bool + job-level `if` + `pull_request` never `pull_request_target`) — every ambiguity fails closed by construction; the coordinator's shallow checks happened to be correct but I verified each independently rather than trusting them.

- 2026-07-27: Reviewed Phase 4 (#20) — TTL reaper (`pr_env_reaper.py`, Trinity 5677e3f) + close-time teardown & daily janitor workflows (Tank e6d2b2e). 🟢 Green, 2 non-blocking advisories. Highest-blast-radius artifact yet: unattended scheduled `az group delete`. PROVED the allowlist adversarially by running the reaper against crafted hostile JSON — a shared/prod RG with a stale `expires-at` but missing/odd-cased/whitespace `ephemeral`/`environment-type` is always KEPT because identity gates run before any expiry check; bool ephemeral, tags:null, tags-as-list, non-ASCII pr-number, and tz-naive expiry all fail-closed to keep; malformed/non-array/bad-now input → exit 3 → `set -e`+`pipefail` fail the run (no delete); empty output → janitor no-op; control chars in names sanitized → no `$GITHUB_OUTPUT` line injection. REASONED (read/grep): only `${{ github.event.* }}` in a run: block is a comment; all data via env; `id-token:write` only on Azure-login jobs; `pull_request` not `_target`; fork job-if skips privileged deletion; every failure mode fails toward KEEPING. Assessed Tank's azd-down→`az group delete` deviation against the bicep: PR stack creates no Key Vault and no Cognitive Services account (both gated on deployFoundry) — the only things `--purge` reclaims — so skipping `--purge` leaks nothing. Advisories: (1) "validated integer" comment for PR_NUMBER has no actual validation (safe by provenance); (2) teardown `az group list` without `set -e` reports az failure as noop-success (fails toward keep). Learning: when an author claims "strictly allowlist-based," prove it by feeding the reaper the exact bypass shapes (stale tag on shared RG, odd casing, naive timestamp) rather than reading the gate order — the empirical run is what turns the claim into evidence.
📌 Team update (2026-07-28T08:22:20+02:00): Rai's Phase 5/#18 review is YELLOW/advisory-only: child `ephemeral=true` tags are non-destructive because deletion tooling reads RG tags and requires `environment-type=pr-app`, which child tags omit. Track durable `author`/`created-at` metadata acknowledgement and the pre-existing non-secure Application Insights connection-string output separately. — reviewed by Rai
- 2026-07-28: Reviewed Phase 6/#17 Foundry exception gate + labelled live validation. 🔴 RED. Live validation hook is clean on HTTP/transport/non-JSON/unexpected/success probes: only pass/fail + correlation ID reached logs, `$GITHUB_OUTPUT`, summary, and PR comment inputs. Preflight coercion is clean inside the current workflow: only exact `foundry_authorized=true` proceeds and cap still blocks. Blocking flaw: privileged deploy checks out/provisions PR-controlled Bicep/workflow code while the detector ignores load-bearing control surfaces (`infra/main.bicep`, `infra/main.bicepparam`, `.github/workflows/pr-environment.yml`, scripts). Local probe returned `requires_foundry=false` for those paths, and repo inspection found no CODEOWNERS/branch protection; GitHub docs confirm `pull_request` runs in merge-commit context. A same-repo PR can alter the root provisioning/workflow path and bypass `azure-foundry-provisioning`. Tank owns workflow/docs fix; Trinity owns detector/tests. Teardown/janitor residual risk acceptable; self-approval is cost-friction/audit, not independent security review.
- 2026-07-28: Re-reviewed Phase 6/#17 after Tank/Trinity fix. 🔴 RED sustained. The accidental same-repo Foundry-spend failure mode is much improved: `deployFoundry` defaults false, `DEPLOY_FOUNDRY` derives from Foundry approval rather than detection, and a pre-provision assertion checks switch wiring. But the trusted boundary is still wrong: `preflight` checks out PR merge content and executes PR-controlled `pr_foundry_scope.py`/`pr_preflight.py`; `deploy` relies only on those outputs and has no independent event-context fork/same-repo job condition before `environment: azure-pr-app` + `id-token: write`. A fork PR can forge `decision=proceed` in the checked-out scripts and reach the Azure credential-bearing job. Required fix: trusted pre-Azure gate (job-level event-context fork/same-repo/draft condition and/or base-ref preflight/detector) before any OIDC job starts.

📌 Team update (2026-07-28T09:03:21Z): Rai accepted Phase 6 as 🟡 with residual risks: PR-controlled preflight/detector, same-repo collaborator workflow/IaC mutation, self-approval cost acknowledgement, and readable non-secret repo vars — decided by Trinity, Tank, Switch, Rai, Fact Checker.
- 2026-07-28: Prepared issue #2 RAI decision assessment for retroactive dev approval of Azure AI Foundry `gpt-image-2` deployment. Verdict 🟡 Yellow: OK for dev with explicit owner acknowledgement of billable provisioning, France Central → Sweden Central cross-region processing, default content/abuse monitoring with possible retention/human review, and swedencentral/GlobalStandard/capacity-1. Not prod-ready without user notice/terms, prompt/output retention policy, stronger content/IP/minor policy, and validated GlobalStandard data-routing terms.

📌 Team update (2026-07-28T10:05:06+02:00): Benoit Moussaud (Product Owner) approved the Foundry stack for development only and retroactively accepted `rg-fantasy-cards-dev-8f327f8c`. Production remains gated on #37 user-facing notice, #36 retention/deletion policy, #38 likeness/IP/minors policy, and #39 revalidate GlobalStandard routing; #39 may invalidate the EU-only assumption behind the approval. Supporting analysis: Tank confirmed Azure state/spend/RAI policy; Rai rated the gate 🟡 YELLOW for dev only.

- 2026-07-28: Recorded corrected `GlobalStandard` routing risk for `gpt-image-2`: inference processing of user prompt text may occur in any Azure region where the model is deployed, including non-EU regions; data at rest remains in the customer-designated geography. Product Owner knowingly accepted the non-EU inference-processing risk for dev and production because EU-bound deployment is not currently purchasable for this model. Rai verdict remains 🟡 Yellow: concern is higher than the earlier EU-only premise, but informed owner acceptance keeps it advisory rather than blocking. Strengthened #37/#36 expectations; #38 unchanged except no EU-only implication.

📌 Team update (2026-07-28T10:14:07+02:00): Risk posture changed from unresolved EU-routing concern to explicit Product Owner acceptance of non-EU GlobalStandard inference for gpt-image-2. Abuse monitoring/content-filter facts remain documented; revisit if data categories, model/SKU, Microsoft routing semantics, or legal requirements change. — decided by Benoit Moussaud
