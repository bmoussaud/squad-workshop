# RAI Audit Trail

> Append-only evidence log. Entries are redacted — never contains raw secrets or harmful content.

<!-- Rai appends findings below -->

### 2026-07-27T14:24:13+02:00: PR environment preflight review
- **Scope:** `infra/scripts/pr_environment_names.py`, `infra/scripts/pr_preflight.py`, and `tests/test_pr_environment_names.py` (`main..HEAD`)
- **Verdict:** 🔴 Red
- **Critical — fail-open trust boundary:** `infra/scripts/pr_preflight.py:94` treats falsey non-boolean fork signals as trusted. Reproduction with absent/ambiguous values and matching repositories returns `PROCEED`. `:78,139` likewise make Foundry-cap enforcement solely conditional on an optional caller-controlled flag, with no explicit-review gate; no sensitive input recorded.
- **Advisory — log safety:** `infra/scripts/pr_preflight.py:121-122` includes an invalid supplied identifier in a printable result message. `infra/scripts/pr_environment_names.py:252,267,352` includes the raw repository input in JSON CLI output. No secrets observed in the diff.
- **Validation:** focused unit suite passed (39 tests); boundary reproductions completed locally.
- **Remediation status:** open; reviewer rejection requires an independent fix owner.

### 2026-07-27T15:15:53+02:00: PR environment preflight re-review
- **Scope:** remediation commit `2292357` (`e8f7688..HEAD`)
- **Verdict:** 🟢 Green
- **Trust boundary closed:** `infra/scripts/pr_preflight.py:118-152` blocks every tested malformed fork/draft/repository signal before draft evaluation. Omission of the required Foundry signal raises before a decision; no fork case proceeded.
- **Foundry control closed:** `infra/scripts/pr_preflight.py:189-213` requires explicit authorization and enforces the cap at/above one. Authorization does not waive the cap.
- **Log safety closed:** `infra/scripts/pr_preflight.py:160-169` emits only opaque names; `infra/scripts/pr_environment_names.py:88-96,374-392` sanitizes errors and emits only allowlisted printable fields. Dynamic newline/control/workflow-command probes produced neither injected lines nor controls.
- **Validation:** full suite passed (148 tests). Diff credential/identifier and terminology scan found no committed secrets or excluded terminology.
- **Remediation status:** verified closed.

### 2026-07-27T17:04:17+02:00: CI PR-ownership gate relaxation review (#25)
- **Scope:** commit `51336ce` — `.github/workflows/ci.yml`, `validate` job "Validate pull request ownership" step (authorized guardrail relaxation).
- **Verdict:** 🟢 Green — relaxation is safe and honestly described.
- **(1) Guarantee lost:** Previously every PR provably closed the issue named in its own conforming branch. Now a non-conforming (app-generated) branch may close ANY single issue with no branch cross-check. In this repo's threat model (human-opened, human-reviewed PRs; `permissions: contents: read`; workshop repo) this is acceptable — the gate is hygiene, not a security boundary, and the reviewer sees which issue is closed. Stated plainly: acceptable, not a meaningful new risk.
- **(2) Conditional cross-check soundness:** As a SECURITY control it is bypassable — an actor evades it by simply not adopting the `squad/{n}-{slug}` name; that is closer to theatre than defence. As a TYPO-CATCHER for contributors who opt into the convention it has real value (catches `squad/16-*` branch closing `#99`). The commit message and code comments describe it honestly ("conforming branches retain the guarantee… non-conforming app branches skip only that comparison"); no overstatement. Honest framing accepted.
- **(3) Injection safety (independently verified, not taken on report):** `BRANCH`/`BODY` are passed via `env:` (lines 17-19) and read as `"$BRANCH"`/`"$BODY"` (21-22); no `${{ }}` interpolation into `run:`. The new `::notice::` echoes attacker-controlled `$branch` mid-line; git refs cannot contain newlines/control chars and GitHub workflow commands are only honoured at line-start, so a crafted ref (e.g. embedded `::error::` or literal `%0A`) cannot start a new command line — no log-command injection. Confirmed by construction + ref-format constraints, not assumed.
- **(4) Fail-open vs fail-closed (empirically tested in bash):** empty/null body → count 0 → exit 1 (fail-closed); conforming match → pass; conforming mismatch → exit 1; non-conforming + one issue → pass (the accepted relaxation); two different issues → exit 1; `grep … || true` yields a 0-length array (no fail-open). No path lets the job pass when it should fail.
- **(5) Double-close false positive:** `Closes #16 … Closes #16` counts as 2 references → exit 1. This is over-strict but fails CLOSED and is PRE-EXISTING (the `-ne 1` count test is unchanged), so it is not a regression from this commit. Recommendation (non-blocking): dedupe references before the count if it becomes a contributor friction point.
- **Advisory (non-blocking):** naive `grep` matches closing keywords inside code fences/quotes; pre-existing, unchanged, fail-closed direction.
- **Validation:** control-flow reproduced in bash across 6 edge cases; injection analysed against git ref-format + Actions command semantics; diff scanned for secrets/terminology — none.
- **Remediation status:** none required. Green with two optional advisories (double-close dedupe; fenced-keyword matching).

### 2026-07-27T20:05:08+02:00: Phase 3 PR deploy workflow + preflight CLI review (#15)
- **Scope:** commit `e0d2d64` — `.github/workflows/pr-environment.yml` (new) + `infra/scripts/pr_preflight.py` CLI layer. Reviewed as UNREVIEWED code of unknown provenance (author lost pre-commit; coordinator salvaged/self-validated). All coordinator claims independently re-verified.
- **Verdict:** 🟢 Green (safe to ship) with two non-blocking advisories.
- **(1) Fork isolation, end-to-end (traced, not asserted):** `head.repo.fork` -> env `IS_FORK` -> `--is-fork "$IS_FORK"` -> `_parse_tristate_bool` (exact `true`/`false` only, else `None`) -> `evaluate` step 0 `_is_bool` gate blocks non-bool BEFORE any other check -> `decision=blocked`. Deploy job `if: needs.preflight.outputs.decision == 'proceed'` is the JOB-level exclusion; blocked/empty/skip never equals `proceed`. Empirically probed is-fork in {true,false,'',TRUE,yes,1,null}: only `false` -> proceed/exit0; every other value -> blocked/exit3. Empty `decision` output (gate step crash: `set -uo pipefail`, no `-e`, no rc check, atomic 3-line emit) -> `'' == 'proceed'` false -> deploy AND comment skipped. Branch content cannot spoof fork status (fork flag is independent of branch; malformed branch -> `invalid_names` BLOCKED). No fork path reaches any Azure step.
- **(2) New CLI trust surface:** `_parse_tristate_bool` fails closed on every non-`true`/`false` input (verified). `_parse_count` uses `str.isdigit()` -> `-1` on empty/non-numeric/negative/whitespace -> `_non_negative_count` rejects -> BLOCKED (empty count probe -> blocked/exit3). Exit contract correct: gate step relies on the `decision` STRING (not exit code) and a separate enforce step turns `blocked` into run failure; SKIP (draft) exits 0 and is NOT treated as PROCEED (draft probe -> `decision=skip`, deploy/comment gated off). Cap-recheck step correctly checks `rc -ne 0` (only proceed/blocked reachable there).
- **(3) Privilege scoping:** workflow default `contents: read`. `id-token: write` ONLY on `deploy`; `pull-requests: write` ONLY on `comment`; `environment: azure-pr-app` bound to `deploy`. `comment` job `if: always() && needs.preflight.outputs.decision == 'proceed'` — its elevated `pull-requests: write` never runs for fork/draft (decision != proceed), and it holds no Azure/id-token scope.
- **(4) Injection & leakage:** no `${{ github.event.* }}` inside any `run:` block — all PR-controlled values via `env:` read as `"$VAR"`. `${{ github.event.pull_request.number }}` appears only in `concurrency.group` and an `az --query` string, both integer-constrained. Comment body omits PR title/author/branch; dynamic fields (`environment_name` via `printable_fields`, Azure FQDN, ISO date, sanitized smoke message) contain no `repo` and no markdown/HTML-breaking chars (slug charset `[a-z0-9-]`). `decision`/`message` env-output injection blocked: `_sanitize_log` strips `\x00-\x1f` (incl. newline) so a crafted branch cannot inject a `decision=proceed` line into `$GITHUB_OUTPUT`. All four third-party actions SHA-pinned (checkout@11bd719, setup-azd@634ad92, Azure/login@a457da9, github-script@f28e40c).
- **(5) Fail-open audit:** preflight crash -> empty decision -> no deploy/comment (fail-safe, no false green health). `azd provision`/`azd deploy` failure -> deploy job fails -> `DEPLOY_RESULT != success` -> comment shows "Deployment did not complete". Smoke failure -> `exit $rc` fails deploy job -> same. Comment failure -> workflow red. No path posts a "Healthy" comment without both deploy success AND `smoke_passed == true`. 186 tests pass (independently run).
- **Advisory 1 (non-blocking, accuracy):** the comment's `⚠️ Smoke test failed — <SMOKE_MESSAGE>` branch is effectively dead: a smoke failure fails the deploy job, so `DEPLOY_RESULT != success` and the "Deployment did not complete" branch renders instead. Direction is fail-CLOSED (understates health), but smoke-specific detail never surfaces. Consider surfacing smoke outcome independently of job result.
- **Advisory 2 (non-blocking, observability):** a preflight/gate step crash yields a green run with a silent no-op (no deploy, no comment). Fail-safe, but indistinguishable from "nothing to do"; consider an explicit empty-decision guard for visibility.
- **Validation:** full suite 186 passed; trust-boundary/exit-code probed empirically across 9 input cases; slug charset, SHA-pins, `permissions:` scoping, and `$GITHUB_OUTPUT` injection surface verified by construction. No committed secrets or excluded terminology.
- **Remediation status:** none required.

## 2026-07-27 — Phase 4 TTL reaper + teardown/janitor workflows (#20) — 🟢 GREEN (2 advisories)
- **Scope:** `infra/scripts/pr_env_reaper.py` (Trinity, 5677e3f), `.github/workflows/pr-environment-teardown.yml` + `pr-environment-janitor.yml` (Tank, e6d2b2e). Highest blast radius in the initiative: scheduled, unattended `az group delete`.
- **(1) Allowlist — PROVED empirically** (ran reaper against crafted hostile JSON, real output): a shared/prod group with a stale `expires-at` but missing/wrong `ephemeral`/`environment-type` is KEPT (`not_ephemeral`/`no_tags`). Identity gates run BEFORE any expiry/closed check, so a stale tag on a non-ephemeral group can never delete it. Exact-match only: `ephemeral` "TRUE"/"True"/" true " and `environment-type` "PR-APP" all → keep. `tags:null` and `tags:[...]` → `no_tags`. JSON `true` (bool) → `not_ephemeral` (non-string rejected). Non-ASCII pr-number → `malformed_pr_number` (no int() crash). Naive (tz-less) `expires-at` on a valid open-PR group → `malformed_expiry` KEEP — never read as "expired long ago". Legit past → reap; legit future → keep. Closed-PR reap correctly precedes expiry.
- **(2) Janitor boundary — PROVED:** empty groups → `reap_names=` empty → delete step no-ops (exit 0). Malformed JSON / non-array / bad `--now` → exit 3 → `set -e`+`pipefail` fail the reap step → NO deletion (fail-closed). Garbage/injection tokens in `--closed-pr-numbers` ("10x, ; rm -rf, 999") dropped → no reap. `reap_names` is space-joined; Azure RG charset `[-\w._()]` has no spaces, so the unquoted `for rg in ${names}` word-split is safe. A name carrying `\n` is collapsed to a space by `_sanitize_log` → single `reap_names=` line, NO `$GITHUB_OUTPUT` line injection.
- **(3) Injection/untrusted input — REASONED (grep-verified):** the only `${{ github.event.* }}` literally inside a `run:` block is teardown L110 (a comment). All PR data crosses via `env:` (`PR_NUMBER`, `CLOSED_PRS`, `REAP_NAMES`, `DRY_RUN`) read as quoted vars. `PR_NUMBER` in the teardown `--query` is a GitHub-assigned integer (provenance-safe); worst case a malformed value breaks JMESPath → az error → fail, never mis-deletes.
- **(4) Permissions/triggers — REASONED (YAML read):** `id-token: write` only on the two Azure-login jobs (teardown `teardown`, janitor `reap`); teardown `comment` job holds only `pull-requests: write`. `pull_request` (never `pull_request_target`); teardown job-level `if: head.repo.fork == false` skips privileged jobs for forks → fork PR close cannot trigger privileged deletion; skipped job = workflow success.
- **(5) Fail-open audit — REASONED:** `gh pr list` failure → `set -e` fails step → no delete. Crashed reaper → exit 3 → no delete. Empty closed list → only expiry reaping (fail toward keep). Teardown `az group list` failure (no `set -e`) → empty `matches` → `status=noop` = fail toward KEEP (janitor backstops). Every failure mode fails toward KEEPING, none toward deleting.
- **(6) Idempotency — REASONED:** both delete loops distinguish `az group delete` success / "group still exists after failure" (`elif az group show` → real failure → `exit 1`) / "already gone" (success). No blanket `|| true`; a genuine delete failure fails the run.
- **azd-down deviation — REASONED against the bicep, not on faith:** PR stack sets `deployFoundry=false`+`deployAcr=false`. `foundry.bicep` gates the Cognitive Services account (`if (deployFoundry)`) and there is NO Key Vault anywhere in the stack — those are the only resource types `azd down --purge` reclaims. Storage + Log Analytics soft-delete behaviour is identical under `az group delete` vs `azd down` and is NOT covered by `--purge` anyway. So skipping `--purge` leaks nothing that `--purge` would have reclaimed: no orphaned cost, no soft-deleted data, no name-reuse block on reopen. Deviation is sound on security merits.
- **Advisory 1 (non-blocking, comment accuracy):** teardown L24/L109 call `PR_NUMBER` a "validated integer" but there is no validation step; it is safe by provenance, not validation. Recommend softening the comment or adding a trivial `[[ "$PR_NUMBER" =~ ^[0-9]+$ ]]` guard (defense-in-depth).
- **Advisory 2 (non-blocking, honesty):** teardown `matches="$(az group list ...)"` runs without `set -e`, so a real `az group list` failure is reported as `status=noop` success. Fails toward keep (safe), but masks an Azure error. Optionally distinguish az failure from empty result.
- **Validation:** 53/53 reaper unit tests pass (independently run); allowlist ordering, boundary exit codes, closed-PR parsing, and `$GITHUB_OUTPUT` injection surface proved empirically across 13+ crafted cases; permissions/trigger/purge claims verified by reading the YAML and bicep. No committed secrets; no excluded terminology (author correctly uses "allowlist").
- **Remediation status:** none required (Green). Two advisories optional.

---

## 2026-07-28 — Phase 5 (#18) per-PR child-resource identity tags — VERDICT: 🟡 YELLOW (advisories only, ship-able)

**Scope reviewed:** commits `38ee838` + `6cba275` on `work` vs `origin/main`. Files: `infra/main.bicep` (new params `ephemeralTag`/`prNumberTag`/`authorTag`/`createdAtTag`, `baseTags`+`union` tag build), `infra/main.bicepparam` (4 `readEnvironmentVariable` bindings), `.github/workflows/pr-environment.yml` (new "Export per-PR child-resource identity tags" step).

### 1. Destructive-action blast radius — 🟢 GREEN (no path to errant deletion)
Proven empirically, three independent barriers stop a shared/dev/prod resource from being reaped via this change:
- **Deletion tooling reads RESOURCE GROUPS only, never child-resource tags.** `pr_env_reaper.py:214-247` evaluates `az group list` entries; janitor `pr-environment-janitor.yml:100` and teardown `pr-environment-teardown.yml:128` and the concurrency cap `pr-environment.yml:250` all query `az group list`. The new params tag CHILD resources (via `var tags` → child modules `main.bicep:153`/`:176`), which no deletion tool ever inspects.
- **The child tag subset omits the gate every deletion tool requires.** Reaper Gate 3 (`pr_env_reaper.py:225`) requires `environment-type == "pr-app"`; janitor/teardown/cap all AND `tags."environment-type"=='pr-app'`. The new subset is `ephemeral/pr-number/author/created-at` only — `environment-type` is deliberately EXCLUDED (`main.bicep:136`). A resource carrying `ephemeral=true` from this path can never satisfy the reap allowlist.
- **`main.bicep` cannot tag its own RG.** `targetScope='resourceGroup'` (`main.bicep:1`); `var tags` flows only to child modules. RG-scope tags (the ones the reaper reads) are stamped exclusively by the workflow at `pr-environment.yml:308-315`, a separate `az tag update` path untouched by this change.
- **Polluted-shell azd-local risk assessed seriously:** `main.bicepparam:10-13` read these via `readEnvironmentVariable`, so a dev exporting `EPHEMERAL_CHILD_TAG=true` and running `azd provision` into a shared RG WOULD cosmetically mislabel that RG's CHILD resources with `ephemeral=true`. But (a) the RG's own tags stay clean, (b) no deletion tool reads child tags, (c) `environment-type=pr-app` is still absent — so this is cosmetic pollution, NOT a destructive-action path. Recorded as Advisory 3.

### 2. PII / sensitive data in tags — 🟡 YELLOW (advisory)
`author` (GitHub login, `pr-environment.yml:172` from `github.event.pull_request.user.login`) and `created-at` become durable Azure child-resource metadata readable by anyone with subscription/RG read, and flow into cost exports + audit logs. GitHub login is already-public pseudonymous identity and the resources are nightly-ephemeral, so exposure is bounded — but stamping a person's identity onto cloud resources is a privacy choice worth an explicit owner sign-off. `created-at` is not PII. No other tag leaks more than intended (`expires-at`/`repo`/`branch` correctly excluded from children). Non-blocking per policy (PII → advisory).

### 3. Untrusted input / injection — 🟢 GREEN (well-mitigated)
`AUTHOR_TAG` is attacker-influenced but handled with the correct pattern: PR author is bound via `env:` (`pr-environment.yml:164` `PR_AUTHOR: ${{ ... }}`) and shell-expanded as `${PR_AUTHOR}` under `set -euo pipefail`, NOT interpolated as `${{ }}` into the run body — so no GitHub-Actions template/command injection. `$GITHUB_ENV` multiline-injection is infeasible: GitHub logins are constrained to `[A-Za-z0-9-]`, ≤39 chars, no newline/`=`/special chars, so no second env var can be smuggled in. Azure tag-value constraints (≤256 chars, no `<>%&\?/`) cannot be violated by a login, a numeric PR number, an ISO timestamp, or the literal `true`. `PR_NUMBER` is an integer from `github.event.pull_request.number`.

### 4. Secret exposure — 🟡 YELLOW (pre-existing, out-of-scope, LOW-MEDIUM)
`main.bicep:213` `output APPLICATIONINSIGHTS_CONNECTION_STRING string = ...` is NOT `@secure()`, so the App Insights connection string (embeds an ingestion InstrumentationKey) is persisted in ARM deployment history in plaintext, readable with RG-read. Pre-existing (not in this diff), so not a gate on this PR. Severity LOW-MEDIUM: it is an ingestion/telemetry key, not a data-plane read credential. Recommend a follow-up to mark it `@secure()`.

**Remediation status:** none required to ship (no RED). Advisories 2/4 recommend follow-ups; Advisory 3 is defense-in-depth only.
**Validation performed:** read reaper + all four RG-tag consumers; confirmed RG-vs-child scope split empirically; confirmed `environment-type` absent from child subset; confirmed `env:`-mapped author handling; confirmed `targetScope='resourceGroup'`. Did NOT re-run `az bicep build` or the compiled-ARM base-tag check (author pre-verified; accepted).

### 2026-07-28T10:05:06+02:00: gpt-image-2 Foundry deployment approval assessment (#2)
- **Scope:** `src/fantasy_cards/adapters.py`, `src/fantasy_cards/web.py`, `src/fantasy_cards/config.py`, `src/fantasy_cards/telemetry.py`, `infra/main.bicep`, `infra/foundry.bicep`, `infra/web.bicep`, `infra/main.bicepparam`.
- **Verdict:** 🟡 Yellow — acceptable for retroactive dev approval with explicit conditions; not sufficient as-is for production approval.
- **Evidence:** app sends user title/description to Foundry; prompt and output safety failures are mapped safely; app logs correlation/outcome/size only, not raw prompts; artifacts are private blobs with 30-day lifecycle; ingress is public with simple rate/concurrency limits.
- **Open RAI risks:** user-prompted image generation, likeness/IP/minor-content risk, Azure default content/abuse monitoring retention and possible Microsoft human review, and cross-region processing from France Central app hosting to Sweden Central Foundry target.
- **Remediation status:** advisory conditions provided to Product Owner; no code/infra modified.

### 2026-07-28T10:28:20+02:00: Accepted risk — gpt-image-2 GlobalStandard non-EU inference processing (#2, #39)
- **Risk accepted:** User-supplied prompt text sent to Azure AI Foundry `gpt-image-2` under `GlobalStandard` may be processed for inference in any Azure region where Microsoft has deployed the model, including non-EU regions such as `eastus2` or `westus3`. This is PROCESSING location risk; data at rest remains in the customer-designated geography.
- **Decision owner/date:** Benoit Moussaud, Product Owner, knowingly accepted this risk on 2026-07-28 after the earlier EU-only premise was corrected.
- **Scope:** accepted for both development and production use of this project, not merely the existing dev deployment.
- **Rationale:** EU-bound inference is not currently purchasable for this model in the live subscription. `gpt-image-2` is not available under `DataZoneStandard` or regional `Standard`; the subscription exposes `GlobalStandard` only. Requiring EU-only inference would mean abandoning this model.
- **Evidence base:** Microsoft Foundry deployment types state that `GlobalStandard` can route to any Azure geography where the model is deployed: https://learn.microsoft.com/en-us/azure/foundry/foundry-models/concepts/deployment-types. Microsoft Azure OpenAI / Foundry data privacy documentation distinguishes processing from data-at-rest geography and describes data handling/content-abuse monitoring: https://learn.microsoft.com/en-us/azure/foundry/responsible-ai/openai/data-privacy. Tank and Fact Checker independently confirmed the corrected routing interpretation for issue #39.
- **Reassessment:** my earlier 🟡 Yellow verdict remains Yellow, but the reason changes. The concern level is higher than my earlier wording implied because inference can leave the EU. It is still not a 🔴 Critical violation under this repo's RAI policy because the Product Owner explicitly made an informed acceptance and the risk is advisory privacy/data-transfer exposure, not a secret leak, injection flaw, or harmful-content bypass.
- **Production conditions after acceptance:** production remains gated on clear user-facing notice, retention/deletion policy, and likeness/IP/minors/content policy. The separate EU-only routing condition is closed as accepted risk, not as mitigated risk.
- **Revisit triggers:** reopen this decision if EU data residency becomes a contractual, customer, regulatory, or procurement requirement; if Microsoft offers `gpt-image-2` under an EU-bound deployment type such as `DataZoneStandard` or regional `Standard`; if the app starts collecting materially more sensitive prompt data; or if Microsoft changes the documented routing/privacy terms.
- **Knock-on effects:** issue #37 notice must explicitly state AI inference processing may occur outside the EU, that prompt text is sent to Microsoft Azure AI Foundry, and that Microsoft content/abuse monitoring may apply. Issue #36 retention policy must cover both application-retained prompts/outputs and provider-side processing/monitoring retention/human-review possibilities; global routing does not otherwise change local artifact retention. Issue #38 likeness/IP/minors policy is substantively unchanged, though it should avoid implying EU-only processing.
- **Newly exposed risk:** cross-border transfer/privacy representation risk. The app must not market or document the service as EU-resident/EU-only while using `GlobalStandard`.
