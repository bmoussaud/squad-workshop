name: fantasy-cards
infra:
  provider: bicep
  path: infra
services:
  web:
    project: .
    language: py
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
hooks:
  postprovision:
    run: python infra/scripts/retire_legacy_storage_blob_role.py
